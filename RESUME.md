# 项目简历话术 · sensor-sim-test

> 本文件专供简历 / 面试使用，提炼自 `architecture.md` 与已落地代码。
> 项目代码、架构契约、评测报告均为真实交付，可经得起追问。

## 一、项目定位（一句话）

一个**轻量自动驾驶仿真测试基础设施**：用 C 生成"假传感器"二进制数据流，Python 做实时数据质量门禁，对外提供 HTTP 测试接口与可视化看板。**零 GPU、不依赖 CARLA**，笔记本即可运行。

投岗方向：智驾仿真测试（主）/ 智能驾驶仿真工程师 / AI 测试 / AI 应用开发。

## 二、我的角色与分工（多智能体协同）

| 模块 | 负责工具 | 我做了什么 |
|---|---|---|
| 架构契约 / 二进制帧协议 | WorkBuddy（我） | 定义帧头 MAGIC、长度域、小端编码、CRC32 覆盖范围、物理范围约束、异常用例清单，写进 `architecture.md` 作为"单一事实源" |
| Python 核心 | WorkBuddy（我） | 帧解析（`protocol.py`）、四类质量门禁（`quality_gate.py`）、SQLite 入库与统计（`database.py`）、FastAPI 接口（`server.py`）、评测框架（`tools/evaluate.py`） |
| C 数据生成器 | Claude Code | 按我的契约深化：真实物理模型（帧间连贯）+ 5 类异常注入 + CLI 参数 + 可复现种子 |
| UI 看板 | Trae | 按我的接口契约做单页原生 JS + Chart.js 实时看板（KPI / 柱状图 / 帧流水表） |

**关键点**：三方通过契约接口解耦，并行开发互不阻塞；我作为架构方保证 C / Python / 前端口径一致。

## 三、技术栈

- **C**：二进制协议实现、传感器物理建模、异常注入、可复现随机数
- **Python**：`struct` 帧解析、`sqlite3`、`FastAPI`、pytest 契约测试
- **前端**：原生 JS + Chart.js（CDN），无框架无打包器
- **工程方法**：多智能体（WorkBuddy / Claude Code / Trae）协同工作流

## 四、核心成果（可量化）

- 自研二进制帧协议 + CRC32 校验，C 与 Python 端严格对齐（8 个 pytest 全过）。
- 质量门禁实时拦截 **4 类损坏异常**：CRC 失败、序列跳变（丢帧/乱序）、物理越界、NaN/非法值。
- **噪声超标类异常按设计放行**（区分"损坏数据"与"边界正常数据"，避免误杀），作为评测漏检点。
- **可复现**：固定随机种子输出完全一致（md5 校验），便于回归测试。
- 实测验收（500 帧，seed=42）：合格率 **76.0%**，拦截分布 CRC_FAIL:23 / SEQ_GAP:53 / OUT_OF_RANGE:61 / NAN_VALUE:89，噪声类正常放行。
- 端到端链路：C 生成器 → Python 消费/门禁 → SQLite → FastAPI → 实时看板。

## 五、简历描述（按需要选）

**精简版（1–2 句）：**
> 从零设计 C-Python 仿真数据管道：定义二进制帧协议与数据质量门禁，实时拦截 CRC / 丢帧 / 越界 / NaN 四类异常，对外提供 HTTP 测试接口与可视化看板；零 GPU 可本地运行。

**详版（STAR）：**
> 为轻量验证智驾感知数据质量，主导搭建仿真测试数据生成器 + 质量门禁。设计二进制帧协议（帧头 / 长度 / CRC / 小端）与异常注入机制，用 C 生成多传感器数据流、Python 做实时门禁；通过契约接口解耦 C 与前端，由多智能体协同并行开发。最终拦截 4 类异常、合格率可量化，落地端到端可复现测试链路。

## 六、面试可展开的要点

- **为什么用二进制协议而不是 JSON？** 贴近真实传感器总线（CAN / 以太网帧），带宽小、解析确定、便于做 CRC 校验与字节级异常注入。
- **门禁为什么放行噪声超标？** 区分"数据损坏"（CRC/NaN/越界）与"边界正常数据"（噪声略超阈值），避免误杀真实边缘场景；同时把噪声类作为评测漏检点，验证门禁不会过度拦截。
- **多工具协同怎么保证一致性？** 单一事实源（`architecture.md`）+ 契约测试（pytest 验证 Python 端解析与 C 端生成字节一致），接口变更先改契约再改实现。
- **可复现怎么做的？** C 侧使用可种子化 PRNG，固定 seed 后同输入同输出，用 md5 校验二进制流一致性，支撑回归测试。
